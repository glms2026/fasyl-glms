// TEMPORARY dev-only scratch file — remove before shipping.
import { RouteLoader } from "./routes/RouteLoader";

export default function DevPreview() {
  return <RouteLoader label="Restoring your session" />;
}
