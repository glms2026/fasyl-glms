# Backend origin, without a trailing slash and without the /api prefix.
# src/lib/config.ts appends /api, matching the Swagger paths (/api/auth/login etc).
VITE_API_BASE_URL=https://fasyl-glms.onrender.com
